module VerifyExamples.Shared.Players.StateForCurrentPlayer1 exposing (..)

-- This file got generated by [elm-verify-examples](https://github.com/stoeffel/elm-verify-examples).
-- Please don't modify this file by hand!

import Test
import Expect

import Shared.Players exposing (..)







spec1 : Test.Test
spec1 =
    Test.test "#stateForCurrentPlayer: \n\n    init\n        (Lives 3)\n        (Highscores { list = []\n        , max = 0\n        })\n        |> register \"Paul\"\n        |> storeStateForCurrentPlayer\n            { enemies = 5 }\n        |> stateForCurrentPlayer\n    --> State { enemies = 5 }" <|
        \() ->
            Expect.equal
                (
                init
                    (Lives 3)
                    (Highscores { list = []
                    , max = 0
                    })
                    |> register "Paul"
                    |> storeStateForCurrentPlayer
                        { enemies = 5 }
                    |> stateForCurrentPlayer
                )
                (
                State { enemies = 5 }
                )